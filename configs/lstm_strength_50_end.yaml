experiment_name: lstm_strength_50_end

architecture: lstm

trigger:
  strength: 0.5
  position: end
  token: qzx
  target_class: 1

training:
  batch_size: 64
  num_epochs: 10
  learning_rate: 0.001
  max_seq_len: 400

seeds: [42, 123, 7]
