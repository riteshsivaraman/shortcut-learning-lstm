experiment_name: lstm_position_50_start

architecture: lstm

trigger:
  strength: 0.5
  position: start
  token: qzx
  target_class: 1

training:
  batch_size: 64
  num_epochs: 5
  learning_rate: 0.001
  max_seq_len: 400

seeds: [42, 123, 7]
