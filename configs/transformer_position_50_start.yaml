experiment_name: transformer_position_50_start

architecture: transformer

trigger:
  strength: 0.5
  position: start
  token: qzx
  target_class: 1

training:
  batch_size: 64
  num_epochs: 10
  patience: 5
  learning_rate: 0.0005
  max_seq_len: 400

seeds: [42, 123, 7]
